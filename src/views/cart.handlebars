<h1>Carrito de productos</h1>
<ul>
  {{#each cart.products}}
    <li>
      <a href="/products/{{this.productId._id}}">
        {{this.productId.title}} - {{this.productId.description}}
      </a>
    </li>
    &nbsp;
  {{/each}}
</ul>

&nbsp;
<a href="/carts/payment/{{cart._id}}">Ir a pagar</a>
